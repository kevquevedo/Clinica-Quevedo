import { FiltropipePipe } from './filtropipe.pipe';

describe('FiltropipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltropipePipe();
    expect(pipe).toBeTruthy();
  });
});
